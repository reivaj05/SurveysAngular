(function () {
    "use strict";

    angular.module("app.accounts", [
        "app.accounts.controllers",
        "app.accounts.services"
    ]);

    angular.module("app.accounts.controllers", []);
    angular.module("app.accounts.services", []);
})();